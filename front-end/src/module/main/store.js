import {fetchItems} from "@/module/main/api/api";

export const store = {
    namespaced: true,
    state: () => {
        return {
        }
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    }
}
